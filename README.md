# Validador de No-show (Streamlit + SQLite)

## Rodar local
python -m venv .venv
# Windows: .venv\Scripts\activate
# macOS/Linux: source .venv/bin/activate
pip install -r requirements.txt
mkdir -p data
streamlit run app.py
